// ShopAdmin is now routed from App.tsx — this file is unused.
// Routes: /admin → OrderTable, /admin/orders/:id → OrderDetail
// Both wrapped in AdminLayout.
export default function ShopAdmin() {
  return null
}
