services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: northshop
      POSTGRES_PASSWORD: northshop
      POSTGRES_DB: northshop
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
